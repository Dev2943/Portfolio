import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import NetflixPortfolio from './netflix-exact-replica';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NetflixPortfolio />
  </React.StrictMode>
);